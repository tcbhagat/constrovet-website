{
  "name": "constrovet-website",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs && npm run test:ocr",
    "test:ocr": "jest --config jest.ocr.config.cjs --runInBand",
    "typecheck:ocr": "tsc -p tsconfig.ocr.json --noEmit",
    "check": "node --check claim-companion/app.js && node --check claim-companion/api.js && node --check claim-companion/calculator.js && node --check claim-companion/extractor.js && node scripts/check-claim-companion.mjs && npm run typecheck:ocr"
  },
  "dependencies": {
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.1.3",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.2"
  }
}
