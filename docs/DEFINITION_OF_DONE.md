# DEFINITION_OF_DONE.md

Stub — Stage 0 skeleton. Per-stage PASS bars, copied from the execution prompt
(v6), to be filled in as each stage is reached. Mirrored to
`../llm-wiki-constrovet/SAFETY_CHECKLIST/DEFINITION_OF_DONE.md`.

## Stage PASS bars (from execution prompt v6)

- **S0**: `docs/SESSION_LOG.md`, `docs/AGENTS.md`, `docs/DEFINITION_OF_DONE.md` exist;
  `../llm-wiki-constrovet/` has its 7 folders locally, unpushed.
- **S1**: regression suite green (current count +1) across all three clients' fixtures;
  diff ready for founder review.
- **S2**: regression suite green against current main (post-S1) across all three
  clients.
- **S3**: all 4 safety scripts exist, documented, run once for real against real data
  from all three clients where scope makes that meaningful.
- **S4**: branch exists, wiki log file exists, and — only after ~21 real days — a
  per-client yes/no readiness verdict with evidence.
- **S5**: all three clients' Stage C (privacy/legal) questions answered with real
  evidence; Prof. Taran has given an explicit MERGE or HOLD.
- **S6**: disable switch located AND test-fired successfully; `phase2-daily-check`
  built and dry-run tested.
- **S7**: each of the 4 data/research crews produces one real output from real data.
- **S8**: `budget-snapshot` and `dod-audit` both run once for real.
- **P1**: weekly dossier filed, every field traced to a real source URL.
- **GATE**: all five GATE_CHECK lines are "yes" with real evidence per client.
- **S9**: each client has explicitly confirmed permission to be referenced BEFORE any
  draft is written using their data.
- **S10**: scoring complete; only ≥7 prospects flagged to proceed.
- **S11**: each send logged with founder's explicit prior approval, one at a time.
